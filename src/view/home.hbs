<ul>
    {{#each posts}}
        <li>
            <h2>{{this.title}}</h2>
            <p>{{this.description}}</p>
            <small>Author: {{this.author}} - Date: {{formatDate this.createDate}}</small>
        </li>
    {{/each}}
</ul>